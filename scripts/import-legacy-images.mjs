// Compatibility entry point retained for older local notes.
// Legacy Markdown has already been rewritten to local /images/articles paths.
// Run `npm run fetch:legacy-assets` once to download missing image files.
console.log('Use: npm run fetch:legacy-assets');
