---
import { legacySeriesRedirects } from '../../config/content-model';

export function getStaticPaths() {
  return Object.keys(legacySeriesRedirects).map((id) => ({ params: { id } }));
}

return Astro.redirect(legacySeriesRedirects[Astro.params.id ?? ''] ?? '/technology/', 301);
---
