# 更新记录

## v0.22

- 将 `D:\work\code\code_o\github_ye\enjoylifeyay` 设为唯一开发目录。
- 推送 `main` 后，GitHub Actions 分别部署 Cloudflare Pages 自定义域名与 GitHub Pages 默认项目地址。
- 为 GitHub Pages 增加 `/enjoylifeyay/` 子路径构建，同时保持主站 canonical 指向自定义域名。
- 完整移除评论组件、评论 API、管理脚本、D1 SQL 与现行评论配置文档。
- 清理重复阅读内容，将新增原则文章归入“原则”，科学常识文章归入“总结”。
- 增加科学资料标识符校验，并核对两篇新增文章的标题层级与元数据。

## v0.21

- 文章分类收缩为 `section + tags`：移除 `domain`、`format`、`topics` 与现有的碎片化系列字段。
- 新增跨栏目标签索引：仅展示至少关联两篇公开文章的标签，并生成对应主题页。
- 将既有文章迁移到唯一的二级栏目路径；旧专题链接自动跳转到对应栏目。
- 新增“工具 → 软件与效率”作为未来 Word、Excel、系统设置和办公技巧文章的归属。
- 二级地图自动隐藏没有真实内容的空栏目，路由仍为以后写作保留。
- 验证草稿移出正式文章目录。
- 将历次零散版本记录合并为本文件。

## v0.20

- 统一 Markdown、文章目录和首页目录的无序列表圆点。
- 修复浅色主题代码块仍使用深色 Shiki 配色的问题。
- 一级页面与站外专题继续由 Markdown 维护。

## v0.19

- 二级地图的父栏目名称改为可点击入口。
- 所有文章展示精确发布时间。
- 清理公开页面中的开发者维护文案。

## v0.15–v0.18

- 将旧个人文章和本地媒体迁入本站。
- 增加运动记录与站外专题。
- 建立五个一级栏目的二级内容地图。

## v0.4–v0.14

- 建立静态站点、文章目录、归档、工具、本地预览和 Cloudflare Pages 发布流程；期间试验过的评论功能已在 v0.22 完整移除。

## v0.21.1

- 修复旧版覆盖升级后遗留的内部验证草稿 `site-notes.md` 导致内容校验失败的问题。
- 本地预览、`npm run check` 与构建前会自动清理这一已废弃的单个内部草稿文件；不会处理其他文章。
