# enjoylifeyay v0.25.1 替换说明

将压缩包中的文件复制到项目根目录，保留目录结构并覆盖同名文件。

本次更新：

1. 所有文章列表删除“一级栏目 · 二级主题 · 标签”层级行。
2. 五个一级栏目页面在简介下方增加“分类”快捷导航，点击后直接进入对应二级主题页面。
3. “分类”和“同级主题”之间不再使用 `·` 分隔，改为空白间距。
4. 更新版本号、测试与变更记录。

替换后执行：

```powershell
npm run check
npm test
npm run build
```
