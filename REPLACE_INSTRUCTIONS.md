# enjoylifeyay v0.25.0 替换说明

1. 将压缩包解压到 `enjoylifeyay` 项目根目录。
2. 保留压缩包内部目录结构，允许覆盖同名文件。
3. 执行：

```powershell
npm run check
npm test
npm run build
```

本更新包只包含 v0.25.0 新增或修改的文件，不包含完整项目、`node_modules` 和构建目录。
